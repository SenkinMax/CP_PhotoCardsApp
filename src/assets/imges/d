<div class="row " >
  <div class="  content col col-sm-6 col-12"
       *ngFor="let users of user ;
       let i = index" >

    <button class = "flippcard ">

      <b>{{users.firstname}} {{users.lastname}} </b>
      <p >{{users.uuid}}</p>
      <p class = " small">{{imag[i]}}</p>
      <p class = " small">{{users.ip}}</p>

    </button>
  </div>
</div>
